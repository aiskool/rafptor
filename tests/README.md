# Tests

Test suites for Rafptor platform components.

## Planned test categories

- `unit/` — Unit tests per module
- `integration/` — Cross-module integration tests
- `fixtures/` — AFP sample files for testing (various complexity levels)
- `benchmarks/` — Performance benchmarks (pages/minute, fidelity scores)
