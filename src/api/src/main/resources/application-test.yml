spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/rafptor-test   # overridden by embedded mongo

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.14

rafptor:
  security:
    jwt-secret: "test-secret-minimum-32-bytes-long-xxxxxxxxxxxxxxx"
  cors:
    allowed-origins: "http://localhost:4041"
